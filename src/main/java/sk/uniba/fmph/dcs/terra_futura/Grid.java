package sk.uniba.fmph.dcs.terra_futura;

import java.util.Optional;

/**
 * Placeholder grid class for the future implementation.
 *
 * The real implementation belongs to the red part (private work).
 * This stub only provides the minimal API needed so that blue classes compile.
 */
public class Grid {

    /**
     * Returns the card at the given position, or an empty Optional if no card is present.
     *
     * @param coordinate grid coordinate
     * @return card at the given position, or empty if no card
     */
    public Optional<Card> getCard(final GridPosition coordinate) {
        return Optional.empty();
    }
}
